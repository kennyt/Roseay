<div class="user_page">
  <ol>
    <div class="user_page_list_header">
      <h4>submissions (<%= @user.submissions.length %>)</h4>
    </div>
    <% @submissions.each do |link| %>
      <div class="user_page_list">
        <%= "#{link.points} points |" %>
        <%= link_to "#{link.song_artist} - #{link.song_name}", link.song_link %>
        <br>
      </div>
    <% end %>
    <div class="user_page_list_header">
      <h4>upvoted (<%= @user.liked_songs.length %>)</h4>
    </div>
    <% @liked_songs.each do |song| %>
    <div class="user_page_list">
      <%= "#{song.points} points | " %>
      <%= link_to "#{song.song_artist} - #{song.song_name}", song.song_link %>
      <br>
    </div>
    <% end %>
  </ol>
</div>