<div class="user_page_top_header">
  <%= link_to 'home', root_path %>
</div>
<br>
<div class="big_header">
  <h1><%= @user.username %></h1>
</div>
<div class="small_header">
  <%= "total (#{@user.total}) | " %>
  <%= "avg (#{@user.avg})" %>
</div>
<%= render @user %>
